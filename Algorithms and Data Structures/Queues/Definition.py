"""A queue is an ordered collection of items where the addition of new items happens at one end, called the "rear",
and the removal of existing items occurs at the other end, commonly called the "front". The ordering principle is
called FIFO, first-in first-out."""


# Operations
"""
1) insert(value, index) where index will always be "0", this will add a new item to the beginning of the list.
2) pop() where the value will be the last will last value in the list. 
"""